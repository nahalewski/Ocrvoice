services:
  - type: web
    name: dialogue-lantern
    runtime: node
    plan: free
    rootDir: web
    buildCommand: npm install && npm run build:render
    startCommand: npm run start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 22.13.0
